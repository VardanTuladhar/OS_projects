#ifndef PROCESS_H
#define PROCESS_H
#include <string>
#include <vector>
using namespace std;
class process{
	public:
		vector <string> Op;
		vector <int> Op_cycles;
		process(string templat);

};
#endif
